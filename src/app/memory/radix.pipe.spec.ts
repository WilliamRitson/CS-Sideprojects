/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RadixPipe } from './radix.pipe';

describe('Pipe: Radix', () => {
  it('create an instance', () => {
    let pipe = new RadixPipe();
    expect(pipe).toBeTruthy();
  });
});
